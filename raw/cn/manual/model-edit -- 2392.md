# 什么是“同步原点”？如何通过同步原点实现旋转/开门动画？

## 概述

在D5渲染器中，模型的坐标轴会默认放置在模型的底部中心，不支持直接修改坐标轴位置，但可结合同步原点与关键帧功能制作开关门动画，具体请参考下面的方法：

1. 选择单独的门的模型，将建模软件中将模型的坐标轴移动到门轴
2. 将门轴放到建模软件世界坐标原点（0，0，0）的位置
3. 将模型导入D5，选择“同步原点”功能
4. 在D5中使用关键帧的方式实现旋转门的开门/关门动画

### 参考视频教程
