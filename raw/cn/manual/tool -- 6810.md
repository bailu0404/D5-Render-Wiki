# 如何使用Cesium?

3. **创建令牌：**  
   a. 进入 `Access Tokens` 页面。  
   b. 点击 `Create token` 创建一个新token（不可使用默认的 `Default` token，因其没有足够的权限）  
   c. 勾选全部 Public scopes 中的数据权限以及需要开放的资产权限。  
   d. **点击`创建`并复制新生成的 token 字符串备用。**
