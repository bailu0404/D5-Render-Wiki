# 如何对齐分次导入的模型？

## 概述

当模型体量较大，面数较高时，使用D5转换器导出 d5a 时可能会占用大量资源，导出的效率较低，或导出失败。 可以先将模型根据场景的需要拆分后，再分别导出。如将建筑主体、地块、配景拆分，再导入D5中进行对齐。

同理，支持直读的建模格式，也可以分块导入对齐。

## 操作详解

### 对齐坐标系

如果模型来源于同一模型文件，可以多选模型后，在右侧边栏选择对齐功能，选中的模型按照原始建模坐标轴归为对齐，保持在建模软件中的坐标相对位置不变。
